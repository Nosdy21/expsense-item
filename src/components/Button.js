import React from 'react';
import './ExpenseItem.css';

const Button = ({text}) => {
    const onClick = () => {}
    return (
    <button 
    onClick = {onClick} 
    className = 'btn'>Click</button>
    )}
    <Description />
export default Button;